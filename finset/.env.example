# ─────────────────────────────────────────────────────────────
#  FinSet – Environment Variables
#  Copy this file to .env for local development:
#    cp .env.example .env
#
#  On Railway: set these in your service's Variables tab.
#  DATABASE_URL is automatically provided by Railway when you
#  attach a PostgreSQL database to your service.
# ─────────────────────────────────────────────────────────────

# PostgreSQL connection string
# Railway format:  postgresql://user:password@host:port/dbname
# Local format:    postgresql://postgres:postgres@localhost:5432/finset
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/finset

# Server port (Railway sets this automatically — don't override on Railway)
PORT=8080
